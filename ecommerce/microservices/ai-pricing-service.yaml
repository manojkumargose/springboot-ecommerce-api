apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-pricing-service
  namespace: ecommerce
  labels:
    app: ai-pricing-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ai-pricing-service
  template:
    metadata:
      labels:
        app: ai-pricing-service
    spec:
      containers:
        - name: ai-pricing-service
          image: your-registry/ai-pricing-service:latest
          ports:
            - containerPort: 8083
          env:
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              valueFrom:
                configMapKeyRef:
                  name: ecommerce-config
                  key: EUREKA_URL
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:mysql://pricing-db.ecommerce.svc.cluster.local:3306/pricing_db"
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: ecommerce-secrets
                  key: DB_USERNAME
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ecommerce-secrets
                  key: DB_PASSWORD
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            initialDelaySeconds: 40
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: ai-pricing-service
  namespace: ecommerce
spec:
  selector:
    app: ai-pricing-service
  ports:
    - port: 8083
      targetPort: 8083